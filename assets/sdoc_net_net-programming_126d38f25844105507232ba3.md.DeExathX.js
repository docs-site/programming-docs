import{_ as o,c as d,o as c,b0 as a}from"./chunks/framework.BWMBvyU7.js";const i="/programming-docs/assets/image-20220630160305540.c34KUhZf.png",s="/programming-docs/assets/image-20220630165320699.CuUwISNc.png",l="/programming-docs/assets/image-20220701063024372.DaBJ5llw.png",t="/programming-docs/assets/image-20220701074211434.BTuZemw3.png",n="/programming-docs/assets/image-20220701083251722.CBZoA9xw.png",m=JSON.parse('{"title":"LV030-网络IO模型","description":null,"frontmatter":{"title":"LV030-网络IO模型","date":"2026-03-16T14:16:36.000Z","icon":"famicons:logo-markdown","permalink":"/sdoc/net/net-programming/126d38f25844105507232ba3","index":true,"tags":null,"categories":null,"copyright":false,"keywords":null,"cover":null,"comments":null,"mathjax":null,"top":null,"description":null,"tdoc":{"detailDate":"2026-03-16T14:16:36.261Z","fulluuid":"507232ba3b28495d883460313ccaeee5","useduuid":"507232ba3"}},"headers":[],"relativePath":"sdoc/net/net-programming/126d38f25844105507232ba3.md","filePath":"sdoc/04-网络相关/20-网络编程/LV030-网络IO模型.md","lastUpdated":1773672539000}'),r={name:"sdoc/net/net-programming/126d38f25844105507232ba3.md"};function p(h,e,k,g,u,I){return c(),d("div",null,[...e[0]||(e[0]=[a('<h1 id="lv030-网络io模型" tabindex="-1">LV030-网络IO模型 <a class="header-anchor" href="#lv030-网络io模型" aria-label="Permalink to &quot;LV030-网络IO模型&quot;">​</a></h1><p>本文主要是网络编程——网络IO模型的相关笔记，若笔记中有错误或者不合适的地方，欢迎批评指正😃。</p><h2 id="一、网络i-o概述" tabindex="-1">一、网络<code>I/O</code>概述 <a class="header-anchor" href="#一、网络i-o概述" aria-label="Permalink to &quot;一、网络`I/O`概述&quot;">​</a></h2><h3 id="_1-五种i-o模型" tabindex="-1">1. 五种<code>I/O</code>模型 <a class="header-anchor" href="#_1-五种i-o模型" aria-label="Permalink to &quot;1. 五种`I/O`模型&quot;">​</a></h3><p>我们日常说的的网络通信本质上其实就是网络<code>I/O</code>，通过网络<code>I/O</code>，我们可以和远程设备进行通信（数据交换）。由于网络<code>I/O</code>和正常的磁盘<code>I/O</code>在性能和访问方式上有较大的差异，所以针对磁盘<code>I/O</code>的读写方法也就无法适用于网络<code>I/O</code>上，大部分操作系统针对网络<code>I/O</code>抽象除了一套特殊的接口—— <strong>网络Socket接口</strong> ，用于对网络<code>I/O</code>进行操作。</p><p>在<code>Linux</code>当中一切皆文件，为了统一概念，<code>socket</code>在<code>Linux</code>当中也是通过文件描述符来进行描述的，只不过这个文件描述符描述的<strong>不是本地文件，而是远程设备对应的文件</strong>。由于网络通信存在不可预知的问题，所以诞生了很多网络<code>I/O</code>模型，这些I/O模型本质上是一种客户端（或者说是服务消费者）对网络<code>I/O</code>请求的处理方式。简单来说，网络<code>I/O</code>本质是 <code>socket</code> 的读写，<code>socket</code> 在 <code>Linux</code> 系统被抽象为流，<code>IO</code> 可以理解为对流的操作。</p><p>对于一次 <code>IO</code> 访问 (以<code>read</code>举例)，数据会先被拷贝到操作系统内核的缓冲区中，然后才会从操作系统内核的缓冲区拷贝到应用程序的地址空间。所以说，当一个 <code>read</code> 操作发生时，它会经历两个阶段：</p><ul><li>第一阶段：等待数据准备 (<code>Waiting for the data to be ready</code>)。</li><li>第二阶段：将数据从内核拷贝到进程中 (<code>Copying the data from the kernel to the process</code>)。</li></ul><p>对于 <code>socket</code> 流而言，</p><ul><li>第一步：通常涉及等待网络上的数据分组到达，然后被复制到内核的某个缓冲区。</li><li>第二步：把数据从内核缓冲区复制到应用进程缓冲区。</li></ul><p>在<code>Linux/UNIX</code>中主要有以下五种<code>I/O</code>模型：</p><table><tbody><tr><td align="center" width="150px">阻塞I/O</td><td align="left">bloking IO，我们最常用的一种I/O模型</td></tr><tr><td align="center" width="150px">非阻塞I/O</td><td align="left">non-blocking IO，可防止进程阻塞在I/O操作上，需要轮询</td></tr><tr><td align="center" width="150px">多路复用I/O</td><td align="left">multiplexing IO，允许同时对多个I/O进行控制</td></tr><tr><td align="center" width="150px">信号驱动I/O</td><td align="left">signal-driven IO，一种异步通信模型</td></tr><tr><td align="center" width="150px">异步I/O</td><td align="left">asynchronous IO，一种异步通信模型</td></tr></tbody></table><p>其实在这五种<code>I/O</code>中，信号驱动<code>I/O</code>用的不是很多，我们见的多数只有四种<code>I/O</code>。</p><h3 id="_2-同步和异步" tabindex="-1">2. 同步和异步 <a class="header-anchor" href="#_2-同步和异步" aria-label="Permalink to &quot;2. 同步和异步&quot;">​</a></h3><ul><li>同步</li></ul><p>所谓同步，就是发出一个功能调用时，<strong>在没有得到结果之前，该调用就不返回或继续执行后续操作</strong>。 简单来说，同步就是必须一件一件事做，等前一件做完了才能做下一件事。</p><ul><li>异步</li></ul><p>当一个异步过程调用发出后，调用者<strong>在没有得到结果之前，就可以继续执行后续操作</strong>。当这个调用完成后，一般通过状态、通知和回调来通知调用者。对于异步调用，调用的返回并不受调用者控制。</p><h2 id="二、阻塞i-o" tabindex="-1">二、阻塞<code>I/O</code> <a class="header-anchor" href="#二、阻塞i-o" aria-label="Permalink to &quot;二、阻塞`I/O`&quot;">​</a></h2><h3 id="_1-场景描述" tabindex="-1">1. 场景描述 <a class="header-anchor" href="#_1-场景描述" aria-label="Permalink to &quot;1. 场景描述&quot;">​</a></h3><p>就像是这样，比如说我们现在要到医院做一个检查，需要拍一个片子，我们拍完之后，拍片子的医生一般就会告诉我们多久后可以来拿结果，但是实际上有很多人都拍了这个片子，医生给的依然是一个大概的时间，其实我们相当于还是不知道什么时候出结果，于是，我们只好坐在拍片子的地方等，直到结果出来，然后才能去找医生询问片子的结果，但是等待的这段时间我们一直都没有离开，这就是一种阻塞。</p><h3 id="_2-基本概念" tabindex="-1">2. 基本概念 <a class="header-anchor" href="#_2-基本概念" aria-label="Permalink to &quot;2. 基本概念&quot;">​</a></h3><p>阻塞式<code>I/O</code>顾名思义就是对文件的<code>I/O</code>操作（读写操作）是阻塞式的，这是最普遍使用的一种<code>I/O</code>模式，大部分程序使用的都是阻塞模式的<code>I/O</code>。</p><p>在默认的情况下，<code>socket</code>套接字建立后处于的模式其实就是阻塞<code>I/O</code>模式，我们之前网络编程中学习的很多函数在调用的过程中都会发生阻塞：</p><ul><li><p>读操作：<code>read()</code>、<code>recv()</code>、<code>recvfrom()</code></p></li><li><p>写操作：<code>write()</code>、<code>send()</code></p></li><li><p>其他操作：<code>accept()</code>、<code>connect()</code></p></li></ul><h3 id="_3-网络模型" tabindex="-1">3. 网络模型 <a class="header-anchor" href="#_3-网络模型" aria-label="Permalink to &quot;3. 网络模型&quot;">​</a></h3><p>我们以<code>read()</code>为例，在这个种<code>I/O</code> 模型中，用户空间的进程执行一个系统调用 <code>read()</code>，从套接字上读取数据，当套接字的接收缓冲区中还没有数据可读，函数<code>read()</code>将发生阻塞。然后它会一直阻塞下去，等待套接字的接收缓冲区中有数据可读。经过一段时间后，缓冲区内接收到数据，于是内核便去唤醒该进程，通过<code>read()</code>访问这些数据。如果在进程阻塞过程中，对方发生故障，那么这个进程将永远阻塞下去。</p><p>在这种模型中，从等待数据到处理数据的两个阶段，整个进程都被阻塞。不能处理别的网络 <code>IO</code>。调用进程处于一种不再消费 <code>CPU</code> 而只是简单等待响应的状态，因此从处理的角度来看，这是非常有效的。</p><img src="'+i+'" alt="image-20220630160305540" style="zoom:50%;"><p>写操作时发生阻塞的情况要比读操作少，<strong>主要发生在要写入的缓冲区的大小小于要写入的数据量的情况下</strong>，这时，写操作不进行任何拷贝工作，将发生阻塞。一旦发送缓冲区内有足够的空间，内核将唤醒进程，将数据从用户缓冲区中拷贝到相应的发送数据缓冲区。</p><p>【注意】<code>UDP</code>不用等待确认，没有实际的发送缓冲区，所以<code>UDP</code>协议中不存在发送缓冲区满的情况，在<code>UDP</code>套接字上执行的写操作永远都不会阻塞。</p><h3 id="_4-优缺点" tabindex="-1">4. 优缺点 <a class="header-anchor" href="#_4-优缺点" aria-label="Permalink to &quot;4. 优缺点&quot;">​</a></h3><ul><li>优点</li></ul><p>开发简单，在阻塞等待期间，用户进程挂起，在挂起期间不会占用 <code>CPU</code> 资源，能够提升<code>CPU</code>的处理效率。</p><ul><li>缺点</li></ul><p>一个进程维护一个 <code>I/O</code> ，不适合高并发，因为一个请求<code>I/O</code>会阻塞进程（线程），所以，需要为每个请求分配一个处理进程（线程）以及时响应，系统开销大。</p><h2 id="三、非阻塞i-o" tabindex="-1">三、非阻塞<code>I/O</code> <a class="header-anchor" href="#三、非阻塞i-o" aria-label="Permalink to &quot;三、非阻塞`I/O`&quot;">​</a></h2><h3 id="_1-场景描述-1" tabindex="-1">1. 场景描述 <a class="header-anchor" href="#_1-场景描述-1" aria-label="Permalink to &quot;1. 场景描述&quot;">​</a></h3><p>还是上边的例子，等结果的这段时间我们就必须一直在这里等着什么也不干嘛？显然可以不用，我们在这个时候可以去旁边的超时转一转或者医院的其他地方转一转，但是我们又急着找医生，想要快点拿到片子，于是我们转一会就去拍片子的地方问一下结果出来没有，来来回回好多次，还不一定等得到结果。这就是非阻塞啦，就是要轮训，不断的问有没有准备好。</p><h3 id="_2-基本概念-1" tabindex="-1">2. 基本概念 <a class="header-anchor" href="#_2-基本概念-1" aria-label="Permalink to &quot;2. 基本概念&quot;">​</a></h3><p>非阻塞式<code>I/O</code>就是对文件的<code>I/O</code>操作（读写操作）是非阻塞式的。当我们将一个套接字设置为非阻塞模式，就相当于告诉了系统内核：当我请求的<code>I/O</code> 操作不能够马上完成时，你想让我的进程进行休眠等待的时候，不可以这么做，需要马上返回一个错误（<code>EAGAIN</code> 或<code>EWOULDBLOCK</code>）给我。</p><p>在一个用户进程中使用了非阻塞模式的套接字，一般来说它需要使用一个循环来不停地测试是否一个文件描述符有数据可读（称做<code>polling</code>）。不停的<code>polling</code> 内核来检查是否<code>I/O</code>操作已经就绪，这将会是一个极浪费<code>CPU</code> 资源的操作。</p><h3 id="_3-网络模型-1" tabindex="-1">3. 网络模型 <a class="header-anchor" href="#_3-网络模型-1" aria-label="Permalink to &quot;3. 网络模型&quot;">​</a></h3><p>我们以<code>recv()</code>函数为例，因为这个函数可以设置为非阻塞模式。在这个种<code>I/O</code> 模型中，用户空间的进程执行一个系统调用 <code>recv()</code>，从套接字上读取数据，当套接字的接收缓冲区中还没有数据可读，函数<code>recv()</code>将返回一个<code>error</code>。进程在返回之后，可以干点别的事情，然后再发起 <code>recv()</code> 系统调用。然后重复上面的过程，循环往复的进行 <code>recv()</code>系统调用。这个过程通常被称之为<strong>轮询</strong>。轮询检查内核数据，直到数据准备好，再拷贝数据到进程，进行数据处理。</p><img src="'+s+`" alt="image-20220630165320699" style="zoom:50%;"><p>【注意】拷贝数据整个过程，进程仍然是属于阻塞的状态。</p><h3 id="_4-优缺点-1" tabindex="-1">4. 优缺点 <a class="header-anchor" href="#_4-优缺点-1" aria-label="Permalink to &quot;4. 优缺点&quot;">​</a></h3><ul><li><p>优点：每次发起 <code>I/O</code> 调用，在内核等待数据的过程中可以立即返回，用户线程不会阻塞，实时性好。</p></li><li><p>缺点：不断轮询内核是否有数据，占用大量 <code>CPU</code> 资源，效率不高。</p></li></ul><h3 id="_5-设置非阻塞模式" tabindex="-1">5. 设置非阻塞模式 <a class="header-anchor" href="#_5-设置非阻塞模式" aria-label="Permalink to &quot;5. 设置非阻塞模式&quot;">​</a></h3><p>当我们一开始建立一个套接字描述符的时候，系统内核将其设置为阻塞<code>I/O</code>模式，那么如何将其修改为非阻塞呢？我们可以通过<code>fcntl()</code>函数或者<code>ioctl()</code>函数：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* fcntl的使用 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> fcntl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> fd</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> cmd</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> arg</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> flag；</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flag </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> fcntl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(sockfd, F_GETFL, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flag </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> O_NONBLOCK;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fcntl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(sockfd, F_SETFL, flag);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ioctl的使用 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b_on </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ioctl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(sock_fd, FIONBIO, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">b_on</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>到目前为止，其实我还没有用过这两个函数，所以这里先简单提一下，以后用到了再详细说明。</p><h2 id="四、多路复用i-o" tabindex="-1">四、多路复用<code>I/O</code> <a class="header-anchor" href="#四、多路复用i-o" aria-label="Permalink to &quot;四、多路复用\`I/O\`&quot;">​</a></h2><p>这部分其实也是本篇笔记的重点，也是学习的重点。</p><h3 id="_1-场景描述-2" tabindex="-1">1. 场景描述 <a class="header-anchor" href="#_1-场景描述-2" aria-label="Permalink to &quot;1. 场景描述&quot;">​</a></h3><p>还是上边的例子，现在的医院其实大多数进行检查的地方都会有一个自助打印机，如果说检查结果出来了，上边就会显示，我们直接自己打印就可以了，就不需要去问拍片子的人结果有没有出来了，这其实就算是一个<code>I/O</code>多路复用的实例。</p><h3 id="_2-什么是i-o多路复用" tabindex="-1">2. 什么是<code>I/O</code>多路复用 <a class="header-anchor" href="#_2-什么是i-o多路复用" aria-label="Permalink to &quot;2. 什么是\`I/O\`多路复用&quot;">​</a></h3><p><code>I/O</code>多路复用（<code>IO multiplexing</code>），它会通过一种机制，可以监视多个文件描述符，一旦某个文件描述符（也就是某个文件）可以执行<code>I/O</code>操作时，能够通知应用程序进行相应的读写操作。<code>I/O</code>多路复用技术是为了解决：在并发式<code>I/O</code>场景中进程或线程阻塞到某个<code>I/O</code>系统调用而出现的技术，使进程不阻塞于某个特定的<code>I/O</code>系统调用。</p><p><code>I/O</code>多路复用存在一个非常明显的特征：外部阻塞式，内部监视多路<code>I/O</code>。</p><h3 id="_3-为什么使用i-o多路复用" tabindex="-1">3. 为什么使用<code>I/O</code>多路复用 <a class="header-anchor" href="#_3-为什么使用i-o多路复用" aria-label="Permalink to &quot;3. 为什么使用\`I/O\`多路复用&quot;">​</a></h3><p>前边我们学习了<code>TCP</code>的多线程服务器和多进程服务器，不管是哪一种，若是新到来一个<code>TCP</code>连接，就需要分配一个进程或者线程，那么随着连接的客户端不断增加，服务器需要维护的进程或者线程数量也是不断增加，这样持续下去，操作系统无论如何是扛不住的。</p><p>既然为每个请求分配一个进程或者线程的方式不合适，那有没有可能只使用一个进程来维护多个 <code>socket</code> 呢？答案是有的，那就是使用<strong>I/O多路复用</strong>技术。</p><p>一个进程虽然任一时刻只能 一个请求，但是如果处理每个请求的事件时，耗时控制在 <code>1</code> 毫秒以内，这样 <code>1</code> 秒内就可以处理上千个请求，把时间拉长来看，多个请求复用了一个进程，这就是多路复用，这种思想很类似一个 <code>CPU</code> 并发多个进程，所以也叫做<strong>时分多路复用</strong>。</p><p><code>I/O</code>多路复用的基本思想是：</p><ul><li>先构造一张有关描述符的表，然后调用一个函数。当这些文件描述符中的一个或多个已准备好进行<code>I/O</code>时函数才返回。</li><li>函数返回时告诉进程那个描述符已就绪，可以进行<code>I/O</code>操作。</li></ul><p>这样其实就很大程度上减轻了操作系统维护大量进程和线程的压力。</p><h3 id="_4-网络模型" tabindex="-1">4. 网络模型 <a class="header-anchor" href="#_4-网络模型" aria-label="Permalink to &quot;4. 网络模型&quot;">​</a></h3><p><code>I/O</code> 多路复用模型会用到 <code>select()</code>、<code>poll()</code>、<code>epoll()</code> 函数，这几个函数也会使进程阻塞，其中<code>select</code> 调用是内核级别的。</p><ul><li><code>select</code>轮询相对非阻塞的轮询的区别在于：<code>select</code> 可以对多个 <code>socket</code> 端口进行监听，当其中任何一个 <code>socket</code> 的数据准好了，就能返回进行可读，然后进程再进行<code>recv()</code> 系统调用，将数据由内核拷贝到用户进程，当然这个过程是阻塞的。</li><li><code>select</code> 相对阻塞<code>I/O</code>不同在于：此时的 <code>select</code> 不是等到 <code>socket</code> 数据全部到达再处理，而是有了一部分数据就会调用用户进程来处理。如何知道有一部分数据到达了呢？监视的事情就交给了内核，内核负责数据到达的处理，也可以理解为&quot;非阻塞&quot;吧。</li></ul><p>具体流程，如下图所示：</p><img src="`+l+'" alt="image-20220701063024372" style="zoom:50%;"><blockquote><p>Tips：流程描述如下：</p><p><code>I/O</code>多路复用就是我们说的 <code>select</code>，<code>poll</code>，<code>epoll</code>，有些地方也称这种 <code>I/O</code> 方式为 <code>event driven IO</code>。<code>select/epoll</code> 的好处就在于单个进程就可以同时处理多个网络连接的<code>I/O</code>。它的基本原理就是 <code>select</code>，<code>poll</code>，<code>epoll</code> 这些函数会不断的轮询所负责的所有 <code>socket</code>套接字，当某个 <code>socket</code>套接字有数据到达了，就通知用户进程。</p><p><strong>当用户进程调用了 select()，那么整个进程会被阻塞</strong> ，而同时，内核会监视所有 <code>select</code> 负责的 <code>socket</code>套接字，当任何一个 <code>socket</code>套接字 中的数据准备好了，<code>select()</code> 就会返回。这个时候用户进程再调用 <code>read()</code>或者一些其他读取数据的操作，将数据从内核拷贝到用户进程。</p></blockquote><p>多路复用的特点是通过一种机制一个进程能同时等待 <code>I/O</code> 文件描述符，内核监视这些文件描述符 (其实就是套接字描述符)，其中的任意一个进入读就绪状态，<code>select</code>， <code>poll</code>，<code>epoll</code> 函数就可以返回。这三个函数又对应着三种监视的方式。</p><p>与阻塞<code>I/O</code>相比。这里需要使用两个<code>system call</code> (系统调用，也就是<code>select()</code> 和 <code>recv()</code>函数)，而阻塞<code>I/O</code>只调用了一个 <code>system call</code>。但是，用 <code>select</code> 的优势在于它可以同时处理多个 客户端的连接。</p><p>所以，如果处理的连接数不是很高的话，使用 <code>select/epoll</code> 的服务器不一定比使用多进程（线程）的服务器性能更好，可能延迟还更大。<code>select/epoll</code> 的优势并不是对于单个连接能处理得更快，而是在于<strong>能处理更多的连接</strong>。</p><p>在<code>I/O</code>多路复用模型中，实际中，对于每一个 <code>socket</code>，一般都设置成为 <code>non-blocking</code>（非阻塞），但是，如上图所示，整个用户的进程其实是一直被阻塞的。只不过进程是被 <code>select()</code>这个函数阻塞，而不是被<code>socket IO</code> 给阻塞了。所以<code>I/O</code>多路复用是阻塞在 <code>select</code>，<code>epoll</code> 这样的系统调用之上，而没有阻塞在真正的 <code>I/O</code> 系统调用如 <code>recv()</code> 之上。</p><p>了解了前面三种 <code>I/O</code> 模式，在用户进程进行系统调用的时候，它们在等待数据到来的时候，处理的方式不一样，直接等待，轮询，<code>select/poll</code> 轮询，两个阶段过程：</p><ul><li>第一个阶段有的阻塞，有的不阻塞，有的可以阻塞又可以不阻塞。</li><li>第二个阶段都是阻塞的。</li></ul><p>从整个 <code>I/O</code> 过程来看，他们都是顺序执行的，因此可以归为同步模型(<code>synchronous</code>)。都是进程主动等待且向内核检查状态。</p><p>高并发的程序一般使用同步非阻塞方式，而非多线程加同步阻塞方式。并发数是指同时进行的任务数 (如同时服务的 <code>HTTP</code> 请求)，而并行数是可以同时工作的物理资源数量 (如 <code>CPU</code> 核数)。通过合理调度任务的不同阶段，并发数可以远远大于并行数，这就是区区几个 <code>CPU</code> 可以支持上万个用户并发请求的奥秘。在这种高并发的情况下，为每个任务 (用户请求)创建一个进程或线程的开销非常大。而同步非阻塞方式可以把多个 <code>I/O</code> 请求丢到后台去，这就可以在一个进程里服务大量的并发 <code>I/O</code> 请求。</p><blockquote><p>IO 多路复用是同步阻塞模型还是异步阻塞模型?</p></blockquote><p>同步是需要主动等待消息通知，而异步则是被动接收消息通知，通过回调、通知、状态等方式来被动获取消息。<code>I/O</code> 多路复用在阻塞到 <code>select()</code> 阶段时，用户进程是主动等待并调用 <code>select()</code> 函数获取数据就绪状态消息，并且其进程状态为阻塞。所以，把 <code>I/O</code> 多路复用归为同步阻塞模式。</p><h2 id="五、信号驱动i-o" tabindex="-1">五、信号驱动<code>I/o</code> <a class="header-anchor" href="#五、信号驱动i-o" aria-label="Permalink to &quot;五、信号驱动`I/o`&quot;">​</a></h2><h3 id="_1-基本概念" tabindex="-1">1. 基本概念 <a class="header-anchor" href="#_1-基本概念" aria-label="Permalink to &quot;1. 基本概念&quot;">​</a></h3><p>我们允许 <code>socket</code> 进行信号驱动 <code>I/O</code>，并注册一个信号处理函数，进程继续运行并不阻塞。当数据准备好时，进程会收到一个 <code>SIGIO</code> 信号，可以在信号处理函数中调用 <code>I/O</code> 操作函数处理数据。</p><h3 id="_2-网络模型" tabindex="-1">2. 网络模型 <a class="header-anchor" href="#_2-网络模型" aria-label="Permalink to &quot;2. 网络模型&quot;">​</a></h3><p>具体过程如下图：</p><img src="'+t+'" alt="image-20220701074211434" style="zoom:50%;"><p>当进程发起一个<code>I/O</code>操作，会向内核注册一个信号处理函数，然后进程返回不阻塞；当内核数据就绪时会发送一个信号给进程，进程便在信号处理函数中调用<code>I/O</code>读取数据。</p><h2 id="六、异步i-o" tabindex="-1">六、异步<code>I/o</code> <a class="header-anchor" href="#六、异步i-o" aria-label="Permalink to &quot;六、异步`I/o`&quot;">​</a></h2><h3 id="_1-基本概念-1" tabindex="-1">1. 基本概念 <a class="header-anchor" href="#_1-基本概念-1" aria-label="Permalink to &quot;1. 基本概念&quot;">​</a></h3><p>相对于同步 <code>I/O</code>，异步 <code>I/O</code> 不是顺序执行。用户进程进行 <code>aio_read()</code>系统调用之后，无论内核数据是否准备好，都会直接返回给用户进程，然后用户态进程可以去做别的事情。等到 <code>socket</code> 数据准备好了，内核直接复制数据给进程，然后从内核向进程发送通知。异步<code>I/O</code> 两个阶段，进程都是非阻塞的。</p><h3 id="_2-网络模型-1" tabindex="-1">2. 网络模型 <a class="header-anchor" href="#_2-网络模型-1" aria-label="Permalink to &quot;2. 网络模型&quot;">​</a></h3><p>Linux 提供了 <code>AIO</code> 库函数实现异步，但是用的很少。目前有很多开源的异步 <code>I/O</code> 库，例如 <code>libevent</code>、<code>libev</code>、<code>libuv</code>。异步过程如下图所示：</p><img src="'+n+'" alt="image-20220701083251722" style="zoom:50%;">',95)])])}const b=o(r,[["render",p]]);export{m as __pageData,b as default};
